# VitaNova Gold MVP - Backend Dependencies
# Install with: pip install -r requirements.txt

# Core Flask
Flask>=2.3.0

# CORS support
flask-cors>=4.0.0

# Authentication
flask-bcrypt>=1.0.1
flask-login>=0.6.2
flask-sqlalchemy>=3.1.0
flask-wtf>=1.2.0

# Token generation (included with Flask, but explicit for clarity)
itsdangerous>=2.1.0

# hCaptcha verification
requests>=2.31.0

# Database
# SQLite is built-in with Python, no additional package needed


# Google Cloud Storage (for persistent storage in GCS bucket)
google-cloud-storage>=2.14.0
